import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <div>
      <h2 className="text-2xl font-bold px-6 py-4">{title}</h2>
    </div>
  );
};

export default SectionTitle;
